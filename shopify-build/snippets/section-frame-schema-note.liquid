{%- comment -%}
  Documentation only — Shopify cannot include schema from snippets.
  Copy the "Shared — Section frame" settings block from m4-te-controls.md
  into each section schema (same label, same order).
{%- endcomment -%}
