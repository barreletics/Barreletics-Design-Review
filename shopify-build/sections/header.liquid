{% comment %}
  Site header — Shopify menus (main + help). CSS/JS in assets/chrome.*
{% endcomment %}

{% assign main_menu = section.settings.menu %}
{% assign help_menu = section.settings.help_menu %}

<header class="site-header" data-site-header role="banner" {{ section.shopify_attributes }}>
  <div class="site-header__inner">
    <button
      class="site-header__hamburger"
      data-mobile-menu-toggle
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="Open menu"
      type="button"
    >
      <svg width="20" height="14" viewBox="0 0 20 14" fill="none" aria-hidden="true">
        <path d="M0 1h20M0 7h20M0 13h20" stroke="currentColor" stroke-width="1.5"/>
      </svg>
    </button>

    <a href="{{ routes.root_url }}" class="site-header__logo" aria-label="{{ shop.name }}">
      {% if section.settings.logo != blank %}
        {{
          section.settings.logo
          | image_url: width: 280
          | image_tag:
            class: 'site-header__logo-img',
            alt: shop.name,
            loading: 'eager',
            widths: '136, 204, 272',
            sizes: '68px'
        }}
      {% else %}
        {%- comment -%} Home WORKING authority: barreletics-logo.png @ 68×22 {%- endcomment -%}
        <img
          class="site-header__logo-img"
          src="{{ 'barreletics-logo.png' | asset_url }}"
          alt="{{ shop.name | escape }}"
          width="68"
          height="22"
          decoding="async"
          loading="eager"
        >
      {% endif %}
    </a>

    <nav class="site-header__nav" aria-label="Main navigation">
      <ul class="site-header__nav-list" role="list">
        {% if main_menu != blank %}
          {% for link in main_menu.links %}
            <li class="site-header__nav-item{% if link.links != blank %} site-header__nav-item--has-sub{% endif %}">
              <a href="{{ link.url }}">{{ link.title }}</a>
              {% if link.links != blank %}
                <ul class="site-header__subnav" role="list">
                  {% for child in link.links %}
                    <li><a href="{{ child.url }}">{{ child.title }}</a></li>
                  {% endfor %}
                </ul>
              {% endif %}
            </li>
          {% endfor %}
        {% endif %}
      </ul>
    </nav>

    <div class="site-header__actions">
      {% if help_menu != blank %}
        <div class="site-header__help site-header__nav-item--has-sub">
          <a href="{{ help_menu.links.first.url | default: routes.root_url }}" class="site-header__action site-header__action--help" aria-label="Help" aria-haspopup="true">
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
              <circle cx="9" cy="9" r="8" stroke="currentColor" stroke-width="1.3"/>
              <path d="M6.5 7a2.5 2.5 0 0 1 5 0c0 1.5-2.5 1.5-2.5 3" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
              <circle cx="9" cy="13" r="0.8" fill="currentColor"/>
            </svg>
          </a>
          <ul class="site-header__subnav site-header__subnav--help" role="list">
            {% for link in help_menu.links %}
              <li><a href="{{ link.url }}">{{ link.title }}</a></li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}

      <a href="{{ routes.search_url }}" class="site-header__action" aria-label="Search">
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
          <circle cx="8" cy="8" r="5.5" stroke="currentColor" stroke-width="1.3"/>
          <path d="M12.5 12.5L16 16" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
        </svg>
      </a>

      <a href="{{ routes.account_url }}" class="site-header__action" aria-label="Account">
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
          <circle cx="9" cy="6" r="3" stroke="currentColor" stroke-width="1.3"/>
          <path d="M3 16c0-3 2.7-5 6-5s6 2 6 5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
        </svg>
      </a>

      <a href="{{ routes.cart_url }}" class="site-header__action site-header__cart" aria-label="Cart ({{ cart.item_count }} items)" data-cart-trigger>
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
          <path d="M4 4h14l-1.5 8H5.5L4 4z" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round"/>
          <circle cx="7" cy="15" r="1" fill="currentColor"/>
          <circle cx="14" cy="15" r="1" fill="currentColor"/>
        </svg>
        <span class="site-header__cart-badge" data-cart-count{% if cart.item_count == 0 %} hidden{% endif %}>{{ cart.item_count }}</span>
      </a>
    </div>
  </div>

  <div class="mobile-menu" id="mobile-menu" data-mobile-menu aria-hidden="true">
    <div class="mobile-menu__overlay" data-mobile-menu-close></div>
    <div class="mobile-menu__drawer" role="dialog" aria-label="Navigation menu">
      <button class="mobile-menu__close" data-mobile-menu-close aria-label="Close menu" type="button">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <path d="M2 2l12 12M14 2L2 14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </button>
      <nav class="mobile-menu__nav" aria-label="Mobile navigation">
        <ul class="mobile-menu__list" role="list">
          {% if main_menu != blank %}
            {% for link in main_menu.links %}
              {% if link.links != blank %}
                <li class="mobile-menu__item mobile-menu__item--parent">
                  <button class="mobile-menu__toggle" aria-expanded="false" type="button">{{ link.title }}</button>
                  <ul class="mobile-menu__sub" role="list">
                    {% for child in link.links %}
                      <li><a href="{{ child.url }}">{{ child.title }}</a></li>
                    {% endfor %}
                  </ul>
                </li>
              {% else %}
                <li class="mobile-menu__item"><a href="{{ link.url }}">{{ link.title }}</a></li>
              {% endif %}
            {% endfor %}
          {% endif %}
        </ul>
        {% if help_menu != blank %}
          <ul class="mobile-menu__utility" role="list">
            {% for link in help_menu.links %}
              <li><a href="{{ link.url }}">{{ link.title }}</a></li>
            {% endfor %}
          </ul>
        {% endif %}
      </nav>
    </div>
  </div>
</header>

{% schema %}
{
  "name": "Header",
  "class": "header-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo image (optional)",
      "info": "Defaults to barreletics-logo.png at mock size (22px tall). Override only if needed."
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Main menu",
      "info": "Theme Editor picker — assign any store menu. Leave blank for logo/actions only."
    },
    {
      "type": "link_list",
      "id": "help_menu",
      "label": "Optional secondary menu",
      "info": "Optional. Shown as help dropdown when assigned. Not required."
    }
  ]
}
{% endschema %}
