{% comment %}
  Cream trust strip — matches live second line.
  Static messages, wrap to 2 lines. No scroll. No rotation.
{% endcomment %}

{% if section.settings.enabled %}
  {% assign slides = section.blocks %}
  {% if slides.size > 0 %}
    <div
      class="announcement-strip"
      role="region"
      aria-label="Promotional messages"
      {{ section.shopify_attributes }}
    >
      <div class="announcement-strip__inner">
        {% for block in slides %}
          {% unless forloop.first %}
            <span class="announcement-strip__sep" aria-hidden="true">|</span>
          {% endunless %}
          <span class="announcement-strip__item" {{ block.shopify_attributes }}>
            {% if block.settings.link_url != blank %}
              <a href="{{ block.settings.link_url }}" class="announcement-strip__link">
                {{ block.settings.text }}
              </a>
            {% else %}
              {{ block.settings.text }}
            {% endif %}
          </span>
        {% endfor %}
      </div>
    </div>
  {% endif %}
{% endif %}

{% schema %}
{
  "name": "Announcement strip",
  "class": "announcement-strip-section",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Show announcement / trust bar",
      "default": true,
      "info": "Cream trust line under the sale banner. Wraps — no scroll, no rotate."
    },
    {
      "type": "range",
      "id": "rotation_speed",
      "min": 2,
      "max": 10,
      "step": 1,
      "unit": "s",
      "label": "Rotation speed (unused)",
      "default": 4,
      "info": "Schema compatibility only. Strip is static like live."
    }
  ],
  "blocks": [
    {
      "type": "message",
      "name": "Message",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Free Shipping Over $150"
        },
        {
          "type": "url",
          "id": "link_url",
          "label": "Optional link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Announcement strip",
      "blocks": [
        { "type": "message", "settings": { "text": "🇺🇸 Made in USA" } },
        { "type": "message", "settings": { "text": "Free Shipping Over $150" } },
        { "type": "message", "settings": { "text": "30 Day Returns", "link_url": "/pages/returns" } }
      ]
    }
  ]
}
{% endschema %}
