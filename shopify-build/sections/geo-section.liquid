{% comment %}
  GEO Content Section (D-022)
  Editable accordion with FAQPage structured data.
  Each page type gets contextually relevant geo content via section settings.
{% endcomment %}

{% assign geo_heading = section.settings.heading | default: 'Trusted in studios everywhere' %}

<div
  class="geo-section"
  style="{% render 'type-override-vars', title_size: section.settings.title_size, body_size: section.settings.body_size, sub_size: section.settings.question_size %}"
>
  <div class="geo-section__container">
    <p class="geo-section__eyebrow">{{ geo_heading }}</p>

    {% for block in section.blocks %}
      {% if block.type == 'geo_item' %}
        <details class="geo-section__item" {{ block.shopify_attributes }}>
          <summary class="geo-section__trigger">
            {{ block.settings.question }}
          </summary>
          <div class="geo-section__content">
            {{ block.settings.answer }}
          </div>
        </details>
      {% endif %}
    {% endfor %}
  </div>
</div>

{% assign geo_blocks_count = 0 %}
{% for block in section.blocks %}
  {% if block.type == 'geo_item' %}
    {% assign geo_blocks_count = geo_blocks_count | plus: 1 %}
  {% endif %}
{% endfor %}

{% if geo_blocks_count > 0 %}
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {% for block in section.blocks %}
          {% if block.type == 'geo_item' %}
            {
              "@type": "Question",
              "name": {{ block.settings.question | json }},
              "acceptedAnswer": {
                "@type": "Answer",
                "text": {{ block.settings.answer | strip_html | json }}
              }
            }{% unless forloop.last %},{% endunless %}
          {% endif %}
        {% endfor %}
      ]
    }
  </script>
{% endif %}

<style>
  .geo-section {
    padding: 0 var(--section-padding-x) var(--section-padding-y);
  }

  .geo-section__container {
    max-width: 900px;
    margin: 0 auto;
  }

  .geo-section__eyebrow {
    font-size: var(--te-title-size, var(--text-sm));
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--text-muted);
    font-weight: var(--type-label-weight);
    margin: 0 0 var(--space-4);
  }

  .geo-section__item {
    border-top: 1px solid #eee8de;
  }

  .geo-section__item:last-child {
    border-bottom: 1px solid #eee8de;
  }

  .geo-section__trigger {
    cursor: pointer;
    font-size: var(--te-sub-size, 12px);
    font-weight: var(--weight-medium);
    color: #9a9182;
    padding: 12px 0;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .geo-section__trigger::-webkit-details-marker {
    display: none;
  }

  .geo-section__trigger::after {
    content: "+";
    font-size: 14px;
    color: var(--border-default);
    flex-shrink: 0;
    margin-left: var(--space-4);
  }

  .geo-section__item[open] .geo-section__trigger::after {
    content: "\2212";
  }

  .geo-section__content {
    padding: 0 0 12px;
    font-size: var(--te-body-size, 12px);
    color: #9a9182;
    line-height: var(--leading-loose);
  }

  @media (max-width: 768px) {
    .geo-section {
      padding: 0 var(--section-padding-x-mobile) var(--section-padding-y-mobile);
    }
  }
</style>

{% schema %}
{
  "name": "GEO Content",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "Trusted in studios everywhere"
    },
    { "type": "header", "content": "Type — size", "info": "Family is brand Type OS. Default keeps the compact SEO-block sizing used on collection and PDP." },
    {
      "type": "select",
      "id": "title_size",
      "label": "Heading size",
      "default": "default",
      "options": [
        { "value": "default", "label": "Default / Type OS" },
        { "value": "11", "label": "11px" },
        { "value": "12", "label": "12px" },
        { "value": "13", "label": "13px" },
        { "value": "14", "label": "14px" },
        { "value": "15", "label": "15px" },
        { "value": "16", "label": "16px" },
        { "value": "17", "label": "17px" },
        { "value": "18", "label": "18px" }
      ]
    },
    {
      "type": "select",
      "id": "question_size",
      "label": "Question size",
      "default": "default",
      "options": [
        { "value": "default", "label": "Default / Type OS" },
        { "value": "12", "label": "12px" },
        { "value": "13", "label": "13px" },
        { "value": "14", "label": "14px" },
        { "value": "15", "label": "15px" },
        { "value": "16", "label": "16px" },
        { "value": "17", "label": "17px" },
        { "value": "18", "label": "18px" }
      ]
    },
    {
      "type": "select",
      "id": "body_size",
      "label": "Answer size",
      "default": "default",
      "options": [
        { "value": "default", "label": "Default / Type OS" },
        { "value": "12", "label": "12px" },
        { "value": "13", "label": "13px" },
        { "value": "14", "label": "14px" },
        { "value": "15", "label": "15px" },
        { "value": "16", "label": "16px" },
        { "value": "17", "label": "17px" },
        { "value": "18", "label": "18px" }
      ]
    }
  ],
  "blocks": [
    {
      "type": "geo_item",
      "name": "GEO Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question / Region heading"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer / Content"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "GEO Content",
      "blocks": [
        {
          "type": "geo_item",
          "settings": {
            "question": "Where are Barreletics worn?",
            "answer": "<p>From reformer studios in Miami to barre classes in NYC, Barreletics are trusted by instructors and athletes across 30+ states and internationally.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
