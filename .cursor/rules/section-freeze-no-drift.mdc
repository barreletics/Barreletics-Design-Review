---
description: Frozen-section registry duties. HARD anti-revert law lives in anti-revert-fail-closed.mdc — CURRENT MESSAGE WINS. NEVER git restore.
alwaysApply: true
---

# Section freeze — no drift (HARD)

**HARD law (read first — FAIL CLOSED):** `.cursor/rules/anti-revert-fail-closed.mdc`  
**Skill (AUTO-INVOKE):** `barreletics-anti-revert` — `.cursor/skills/barreletics-anti-revert/SKILL.md`  
**Hooks:** `.cursor/hooks.json` blocks `git restore` / path checkout on guarded files  
**Registry:** `planning/m4-section-freeze.md`
**Contract:** `planning/m4-section-library-CONTRACT.md` §8  
**Copy law (sole wording):** `.cursor/rules/sole-description-language.mdc` — never "fully enclosed"; never a discipline split

## Supreme law (repeat)

**Freeze doc vs Andrew’s CURRENT message → CURRENT MESSAGE WINS.**  
Update the freeze **forward**. Never silently restore an older freeze over his ask.  
**NEVER** `git restore` / `git checkout` sections or templates to “fix drift.” If unsure → **STOP and ASK**.

**Recently abused (radioactive without “restore X”):** `home-juicer` (Instagram) · `proof-numbers` · `value-strip` / under-ATC / badges · footer · `pdp-buy-box` · `index.json` · `product.json`.

## Registry snapshot (verify file — do not invent)

| Item | Status | Agent duty |
|------|--------|------------|
| **Footer** | See freeze registry (light Join the list + TE checklist; NO 10%) | Match **current** freeze row + current message — not obsolete charcoal A+ comments |
| **PDP** | **v19 Locked + product.json 2026-08-01** (current) · **Closed badge rust 2026-08-11** · **v16 Locked** prior (691f03b). July 17 ≠ hub current. | Match v19 mock + current `product.json` spine on draft `187144929571`. **Closed Sole badge = rust** (never charcoal). Keep v16 + v19 locked HTML. **Never overwrite locked mock files — new versions only.** Spine: buy→**4-up Free shipping strip**→features→disciplines→sock-era→variants→lifestyle quote→fullbleed TRANSFORM→compact sock-math→fullbleed-lifestyle wow→commit→social-proof→Think outside the sock→guarantee 3-up→juicer→FAQ+GEO→sticky. Empty under ATC · accordion = policy detail. No page studio-trust / newsletter. |
| **Type OS** | SETTLED | No font_picker sprawl |
| **Home WORKING** | Layout authority | No alternate homepage invention |

## HARD — require Andrew letter in CURRENT message

- Change frozen **structure** (markup, visual system, composition)  
- Change PDP template / buy-box / stack without gallery letter when required  
- Reintroduce stripped copy (brand blurb when removed, **10%**, etc.)  
- **`git checkout` / `git restore` / overwrite** of `shopify-build/sections/**` or `templates/**` from older commits — needs **“restore X”**  
- Silent revert of Instagram / Juicer / proof-numbers / value-strip / under-ATC / badges  
- Replace footer/header/home/PDP with Impulse, live theme, dark Phase 1, or gallery pick without a letter  
- Treat `docs/footer-version-gallery.html` B–H as deployable replacements without a letter  

## Allowed without re-approval

- Bug fixes that preserve the **current** frozen composition in the registry  
- TE label/copy clarifications that do not change layout  
- Deploy only to Andrew-named disposable draft ID in that same message  

## Pre-edit checklist (footer / product.json / index.json / freeze-listed files)

1. Read `planning/m4-section-freeze.md`  
2. State what will change  
3. Never restore prior composition unless Andrew says **“restore X”**  

If unsure: **STOP and ask.**
