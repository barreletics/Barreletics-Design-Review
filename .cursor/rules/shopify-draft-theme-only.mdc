---
description: HARD RULE — only edit Shopify draft theme 187143618851; never live or other backups
alwaysApply: true
---

# Shopify theme safety (HARD — non-negotiable)

## Only allowed Shopify theme target
- Store: `barreletics`
- **ONLY** theme ID: `187143618851`
- Editor: https://admin.shopify.com/store/barreletics/themes/187143618851/editor

All `shopify theme push`, `pull`, `dev`, `delete`, publish, rename, or Theme Editor API changes MUST use this ID (or fail closed and ask).

## Forbidden without explicit owner approval in the current message
- The **live / published** theme (any ID that is Current)
- Any **other** Theme library copy, backup, duplicate, or “backcopy” of live
- Local donor folder used as a push target: `/Users/andrewnehra/barreletics-theme-live-apr2026` (read-only reference only)
- Publishing any theme
- Pushing without `--theme 187143618851` (or equivalent explicit draft ID flag)

## If ambiguous
STOP and ask. Never guess which theme is live. Never “just push to the store.” Never use a second draft/backup “to be safe.”

## Local code
- `shopify-build/` and this repo may be edited freely.
- Live theme on disk is **reference only** — do not push from it unless Andrew explicitly names draft `187143618851` as the destination in that same request.
