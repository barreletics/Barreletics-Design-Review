---
description: HARD LOCK — cream bands use soft #faf8f6 only; match sock-era; never darker cream or silent white
alwaysApply: true
---

# Cream band LOCKED (sitewide)

**Token:** `#faf8f6` (`--color-warm-cream-soft` / `sitewide.lock.json` → `tokens.cream`)

## Law
1. **Disciplines** (“Upgrade your grip / workout”) cream = **`#faf8f6`** — same as **sock-era** (`The Pilates sock era is over.`) on that PDP.
2. **NEVER** `#f5f2ec` (darker warm cream) for marketing/typographic cream bands.
3. **NEVER** flip a cream band to **white** to “fix double cream” without Andrew’s letter in the **current** message.
4. If two creams stack: lighten by using **one** soft `#faf8f6` + white neighbors — or ask. Do **not** invent a third cream hex.
5. **Fix-forward.** Do **not** `git restore` / checkout older `product.coperni.json` to “get back” cream. CURRENT MESSAGE + this lock win.
6. Before push: confirm disciplines `bg_color` and sock-era `bg_color` match on the template you edited.

## Ruler
PDP: `fifty-fifty-sock-era` → `bg_color: #faf8f6`  
Scanner / OS: `planning/locks/sitewide.lock.json` → `cream` + `cream_bands`

## Companion
`anti-revert-fail-closed.mdc` · `no-drift-push-gate.mdc` · Coperni PDP lock 2026-09-13
