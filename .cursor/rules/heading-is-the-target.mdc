---
description: HARD — The heading he named is the only section. One pass. Pull TE before any JSON. Never remap to a neighbor.
alwaysApply: true
---

# Heading is the target (HARD)

**Owner 2026-09-09.** Agents remapped “yellow image” to the wrong 50/50 and pushed JSON over Andrew’s Theme Editor save. That is forbidden.

## Target = the heading he said

If he names a heading, that block is the only surface.

```
✅ “Never slip in chair pose”
   → fifty-fifty-lifestyle on Open · that image only
❌ Neighbor “Never loses shape” because it also has a yellow photo
```

No neighbor. No “same kind of section.” No second 50/50.

If the heading is not on the page → **STOP. Ask.**

## One pass

Do the named items in that one section. Verify **that** heading + **that** media at 1440 and 390. Then stop.

A second pass on a neighbor = you missed.

## Never overwrite his save

Draft `187144929571` Theme Editor is his.

1. Pull the template he is on **before** any JSON edit. Open live suffix = `product.in-studio-template.json`.
2. Change only the named keys.
3. Keep his images, crop, copy, order.
4. Push JSON only after that pull. Liquid-only when JSON is not required.

```
❌ Push local product*.json over TE
❌ Guess which 50/50 has the yellow shoe
❌ “While I’m here” on Never loses / Knock / sock era
```

## Open / Closed Display type

50/50 Display headings + “Tired of slipping in your yoga socks?” = **400**.  
TRANSFORM over video = **500** (leave).

## Product 50/50 frame

Desktop **560** · phone **550**.

**Never slip in chair pose** yellow = `fit` · scale **100**. Whole photo in the frame. **Never cover.** Do not change other 50/50s to match it. See `chair-pose-yellow-fit.mdc`.

Companion: `chair-pose-yellow-fit` · `use-chair-pose-yellow-lock` · `click-the-thing-he-sees` · skill `barreletics-use-chair-pose-yellow-lock`.
