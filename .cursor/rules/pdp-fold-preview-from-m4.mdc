---
description: PDP fold / spacing previews must match current M4 buy-box — never clone Locked v19
alwaysApply: true
---

# PDP fold previews — FROM CURRENT M4 (HARD)

**Owner 2026-08-09:** Fold / breathing-room mocks cloned from Locked `Definitive-v19.html` looked like **reverts** (qty stepper, Coming soon S, 4 thumbs, old stack). That is forbidden.

## NEVER

- Clone `docs/Barreletics PDP - Definitive-v16.html` or `…-v19.html` as the base for a fold / spacing / buy-column experiment
- Reintroduce in fold previews: quantity stepper, Coming soon **S**, thumb cap of 4, or any buy-box UI removed from current `shopify-build/sections/pdp-buy-box.liquid` + `templates/product*.json`

## ALWAYS

1. Base fold / spacing previews on **current M4 buy-box**:
   - `shopify-build/sections/pdp-buy-box.liquid`
   - `shopify-build/templates/product.json` (and open/outdoor as needed)
2. **New version file** or **edit the existing fold preview in place** — never overwrite Locked v19/v16
3. Preview-only until Andrew says **LOCK THIS** + URL or “push to M4”
4. Working fold **proportions**: `docs/Barreletics PDP - Definitive-v20c-fold-breathe-more.html` (buy column only — **not** fold judgment)
5. **LOCKED fold judgment with chrome** (ATC above fold): `docs/pdp-fold-v20d-chrome.html` / `Definitive-v20d-fold-with-chrome.html` — announcement + sticky header + fold line. Never judge fold on naked v20c alone.

## v20d chrome LOCK (2026-08-11) — NEVER thrash

Hub authority for fold judgment. **Do not change** unless Andrew’s CURRENT message is chrome / fold-chrome work:

| Element | Locked |
|---|---|
| Logo | `barreletics-logo.png` · **42px** height (~129×42) |
| Nav type | **#2 — 13px / 500 / sentence case** mid Roboto (not 11px caps, not Type OS 18px) |
| Parent nav | Grippy Footwear · Hot Pilates & Yoga Kits · Apparel · Collaborations · Journal |
| Actions | Help ▾ · Account · Cart |
| Buy type | Roboto · v20c buy-column facts |

Sitewide header **synced 2026-08-11** to nav #2 (`--type-nav-*` 13px/500 · `header-group` nav_link_size 13). Logo TE height left at 45 (do not force 42 without letter).

## Conversion (HARD — rebuild strategy)

- **Never hide colors** behind horizontal swipe / overflow-only rows to win a fold screenshot
- All swatches stay **visible** (`flex-wrap` OK). Fold pressure → tighten stack above ATC + sticky ATC — not conceal options
- Color → Size → ATC (never ATC before options)

## Current M4 buy-box facts (verify file — do not invent)

- Color → size buttons → ATC (never ATC before options)
- Size row header = **chart-only** (`Size Chart →`) — **no** “Size” label (v20c)
- `show_soon_size: false` → **M / L only**
- No quantity control in buy-box
- `thumb_count: 7`
- Swatches: Shopify Files `{handle}.png` / CSS color fallback · wrap visible
- Empty under ATC (kits/accordion below; no ✓ trust row under ATC unless TE on)
- Buy column stack gap ~14px (v20c) so ATC stays above the fold

## If unsure

STOP and ASK. Do not “start from v19 and tweak.”
