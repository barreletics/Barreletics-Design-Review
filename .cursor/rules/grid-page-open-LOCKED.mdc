---
description: HARD — Grid page-open under-nav pad is locked to Shop All 72/56. Never invent.
alwaysApply: true
---
# Grid page-open pad LOCKED

**Ruler:** Shop All collection (`templates/collection.json` / barre-pilates-yoga-shoe-sock-footwear).

| | Desk | Phone |
|--|------|-------|
| Under nav (grid opens page) | **72** (`--pad-grid-page-open`) | **56** (`--pad-grid-page-open-mobile`) |

**Code:** `assets/design-tokens.css` + `sections/variant-grid.liquid` (`section.index == 1`).

**Do not** invent a different top pad for Best Grippy Socks or any other grid-first page.
**Do not** rely on Theme Editor checkbox alone — Liquid index gate is the lock.
