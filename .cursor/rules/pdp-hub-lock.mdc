---
description: HARD — PDP hub Locked mock + draft spine must never be overwritten or swapped without LOCK THIS + URL
alwaysApply: true
---

# PDP hub lock — NEVER OVERWRITE (HARD)

**Current authority (2026-08-01 Andrew letter — lock current PDP composition):**  
1. Mock: `docs/Barreletics PDP - Definitive-v19.html`  
2. Draft spine: theme **`187144929571`** ↔ `shopify-build/templates/product.json` (**living spine** for Aug 1 refinements — 4-up Free shipping strip · TRANSFORM · lifestyle wow)

**Prior fingerprint (NEVER overwrite):**  
`docs/Barreletics PDP - Definitive-v16.html` @ git **`691f03b`** (BZ-020 type calm · Jul 24) — product lede `clamp(34px, 4vw, 44px)` / **400**, Complete the kit, Coming soon (S).  
**NOT** Type OS hero-72 rewrite (`236a001`). That overwrite is archived only as:  
`docs/Barreletics PDP - Definitive-v16-TypeOS-236a001.html`

**Backups (v16 identical to lock):**  
- `docs/pdp-signed-backups/Barreletics PDP - Definitive-v16 @ 691f03b (pre-Type-OS).html`  
- `barreletics-design-review/design_handoff_barreletics 2/pages/Barreletics PDP - Definitive-v16.html`

## Locked spine (`product.json` order)

`pdp-buy-box` → `value-strip` → `pdp-features` → `disciplines` → `fifty-fifty-sock-era` → `variant-grid` → `fifty-fifty-lifestyle` → `fullbleed-statement` (TRANSFORM) → `pdp-sock-math` → `fullbleed-lifestyle` (media-only wow) → `fifty-fifty-commit` → `social-proof` → `fifty-fifty-numbers` → `guarantee-band` → `home-juicer` → `collection-faq` → `pdp-sticky-atc`

**Trust (2026-08-01):** under ATC empty · strip scan = Made in USA · Free shipping over $150 · 30-day returns · 90-day warranty · accordion = policy detail

**Hard absences:** no page `studio-trust` · no page `newsletter` · reviews = `social-proof` (not `pdp-reviews`)

## Forbidden without `LOCK THIS` + new URL in the CURRENT message

1. Edit / overwrite `docs/Barreletics PDP - Definitive-v16.html` or `Definitive-v19.html` in place  
2. Flip hub PDP card off v19 to July 17 / v17 / TypeOS-236a001 / any other mock  
3. “Restore” Type OS hero sizing onto the buy-box lede of the v16 lock  
4. Strip Complete the kit / Coming soon to match July 17  
5. Treat `APPROVED July 17` as hub current (Archive / rejected for hub authority)  
6. Thrash `product.json` spine / section order without Andrew letter

## Allowed

- New version files only (`Definitive-v20.html`, etc.) for experiments  
- Draft Shopify match / redeploy when theme ID named in-message (`187144929571` default QA)  
- Bug fixes that preserve locked compositions  
- Tweaks only when Andrew directs in the current message

## Hub cards (2026-08-01)

**Current authority:** `docs/index.html` → Shop → **PDP v19 · Locked** → `Barreletics PDP - Definitive-v19.html` + current `product.json` (Aug 1 refinements)  
**Prior fingerprint (never overwrite):** **PDP v16 · Locked / APPROVED** → `Barreletics PDP - Definitive-v16.html` @ `691f03b`  
Registry: `planning/m4-section-freeze.md` · `planning/PDP-WORKING-ENTRY.md` · `specs/frozen/pdp.md`
