---
name: barreletics-heading-is-the-target
description: >-
  HARD. Auto-invoke when Andrew names a heading, a 50/50, a yellow/chair
  photo, or says one pass / don’t overwrite my save. The heading he said
  is the only section. Pull TE first. Never remap to a neighbor.
---

# Heading is the target

Andrew 2026-09-09: *why does it take more than one pass* · *don’t overwrite my save* · *it’s Never slip in chair pose*.

## Sense it — STOP

You are about to fail if you:

- Pick a different 50/50 because the photo looks similar
- Push `product*.json` without pulling TE on this turn
- Touch Never loses when he said Chair Pose (or the reverse)
- Call it done from JSON without opening **that** heading

## Gate

```
HEADING GATE
- [ ] Wrote the exact heading he said
- [ ] Opened THAT section only
- [ ] Pulled TE (Open = in-studio-template) before JSON
- [ ] Did not change a neighbor
- [ ] Verified that heading + that media at 1440 and 390
```

## Order

1. Quote his heading.
2. Pull the live template.
3. Edit only that section’s named keys.
4. Screenshot that block. Stop.

## Open Sole facts

- Chair Pose = `fifty-fifty-lifestyle` · title **Never slip in chair pose**
- Yellow shoe on **Never loses** is a different section. Do not remap.
- Display 50/50 + Tired of slipping = **400**. TRANSFORM = **500**.
- **Never slip in chair pose** yellow = `fit` · **100**. Whole photo in the frame. **Never cover.** Do not change other 50/50s to match it. Skills: `barreletics-chair-pose-yellow-fit` + `barreletics-use-chair-pose-yellow-lock`.
