# PDP global changelog

**Purpose:** Every **global** PDP change (buy-box, fold chrome, badges, accordion, value-strip, shared section Liquid used on all PDPs, etc.) gets one line here, then a **Track C sync sweep** across every PDP template.

**Templates in scope:**  
`product.json` (Closed) · `product.open-sole.json` · `product.outdoor.json` · `product.one-off-closed.json` · `product.one-off-open.json`  
(+ shared `sections/*.liquid` when the change is Liquid/schema)

**Statuses after a log line:** open Track C sync row → apply everywhere it applies → AWAITING each / batch → Andrew approves → LOCK.

---

| Date | What changed | Files | Sync status |
|------|--------------|-------|-------------|
| *(none yet this sprint)* | — | — | — |

---

## How to append

```
| YYYY-MM-DD | Short description | paths | QUEUED / IN PROGRESS / DONE |
```

Never fix one PDP globally and leave stragglers.
